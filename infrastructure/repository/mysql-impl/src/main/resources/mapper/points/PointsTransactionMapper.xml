<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awsome.shop.point.repository.mysql.mapper.points.PointsTransactionMapper">

    <!-- 分页查询某账户的交易记录 -->
    <select id="selectPageByAccountId" resultType="com.awsome.shop.point.repository.mysql.po.points.PointsTransactionPO">
        SELECT id, account_id, type, amount, balance_after, description,
               operator_id, related_order_id, created_at
        FROM points_transaction
        WHERE account_id = #{accountId}
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
